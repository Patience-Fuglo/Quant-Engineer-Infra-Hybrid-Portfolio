FROM mcr.microsoft.com/devcontainers/python:3.11

# Upgrade pip
RUN pip install --upgrade pip

# Optionally install requirements if the file exists at runtime (see devcontainer.json for postCreateCommand)